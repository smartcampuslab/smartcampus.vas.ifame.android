<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp" >

    <TextView
        android:id="@+id/isoldi_upper_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/credito_residuo_isoldi"
        android:textSize="30sp" /> 

    <ListView
        android:id="@+id/isoldi_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/isoldi_bottom_text" />

    <TextView
        android:id="@+id/isoldi_center_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/isoldi_upper_text"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="49dp"
        android:text="hello"
        android:textSize="70sp" />

    <TextView
        android:id="@+id/isoldi_bottom_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/isoldi_listview"
        android:layout_below="@+id/isoldi_center_text"
        android:layout_marginTop="16dp"
        android:text="hello"
        android:textSize="25sp" />

</RelativeLayout>